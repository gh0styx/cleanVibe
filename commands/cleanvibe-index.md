# CleanVibe Index

Refresh the CleanVibe architecture index and show a concise summary.

Use the `cleanvibe` skill and the `run_cleanvibe_index_summary` MCP tool:

1. Call `run_cleanvibe_index_summary` for the current workspace.
2. Return the Markdown summary exactly as the tool returns it.

This command is for checking whether CleanVibe sees the expected TypeScript/JavaScript project surface. Do not modify files.
